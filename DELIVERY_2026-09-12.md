# September 12 delivery

Newton authorized fixes, testing and deployment. This supersedes earlier pause and unbuilt-admin notes.

StorageBuddy is the brand operated by FLOWCRAFT PROCESS SOLUTION CO.,LTD. Public legal/address/tax details are updated. Original logo is unchanged from the supplied logo.png. Public contacts: 062-289-5651, nathaphon.u@flowcraftprocesssolution.com and @storagebuddy. assets/line_official.jpg is the exact supplied Line Official.jpg, with a scan/tap contact card.

## Owner back office

https://storagebuddyth.com/backoffice uses the existing CCAQR owner username and password. Authentication is shared with CCAQR, not a separately copied password. Only an active owner profile may access reporting through functions/api/backoffice.js. Sessions use sessionStorage; passwords are not saved. Sign-out is local to this session.

The existing Storage Supabase database remains in use. STORAGE_BACKOFFICE_KEY is encrypted in Cloudflare production configuration; never place its value in files or browser code. The server validates the CCAQR token and owner role before querying Storage. Public clients cannot read leads/events/followups.

backoffice_setup.sql was applied initially. backoffice_owner_auth.sql was subsequently applied successfully to revoke the superseded Storage email-login read route and retain service-role access. Do not apply the initial migration alone as the final access configuration. Locked Flowcraft archive untouched.

Dashboard includes page views, anonymous sessions, enquiry/area-vote counts, source/activity breakdown, contact clicks, AI activity, saved contact details and timestamps, statuses, contact/follow-up dates, notes and CSV export. Test records are excluded by default. Anonymous sessions are not a count of identified people.

## Verification

Real production photo uploads passed in Thai and English using the document-storage photo. Both returned actual provider estimates identifying three cardboard boxes. Estimate-to-enquiry submission returned 201 and was visible in the owner dashboard. The marked TEST ONLY ACCEPTANCE 20260912 record uses setup_test_acceptance_20260912; no customer was contacted. Follow-up status, dates and notes persisted across a full reload. Real CCAQR owner password login and sign-out passed.

Fixed interrupted auto-analysis when typing after photo selection and mixed valid/invalid upload handling. Tested both locally, plus mobile/desktop layout. Provider/model remains claude-sonnet-5. Do not repeatedly consume the five-estimates-per-IP/hour quota for routine UI changes. No physical-phone camera testing claim.

Seven backend tests passed. Logo/legal/contact layout checks passed at 360/390/768/1440 widths. Browser test scripts and screenshots are in the local Temp/storagebuddy-review-tools directory, outside published assets.

CCAQR companion fix is commit 7ed0ed6 in newton-life-os: late Pricing load no longer replaces successful customer PO fallback data with an empty shared result. Race regression tests passed. Live S0022 search for ข้อ retained the July PO price 85 alongside Sales 95 and 75 after all 1116 prices loaded.

Keep build.cjs as the explicit public allowlist; SQL, credentials and internal handoffs must never be published.
